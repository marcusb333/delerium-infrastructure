# Environment Configuration Template
# File: .env.example
# Version: 1.0.0
#
# Copy this file to .env and fill in your values
# cp .env.example .env

# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================

# Deletion Token Pepper (REQUIRED for production)
# This secret is used to hash deletion tokens. Must be kept secret!
# Generate with: openssl rand -hex 32
DELETION_TOKEN_PEPPER=change-me-in-production

# GitHub Username (for repository references)
GITHUB_USERNAME=marcusb333

# Docker Hub Username (for container images)
DOCKER_USERNAME=marcusb333

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# Server Configuration
SERVER_VERSION=latest
LOG_LEVEL=INFO
SERVER_PORT=8080

# Web Server Configuration
WEB_PORT=8080
CLIENT_DIST=./client-dist

# Data Storage
DATA_DIR=../data
LOGS_DIR=../logs

# SSL/TLS Configuration (for production)
SSL_CERT_PATH=../ssl
SSL_DOMAIN=example.com
SSL_EMAIL=admin@example.com

# =============================================================================
# ADVANCED CONFIGURATION
# =============================================================================

# Resource Limits
SERVER_MEMORY_LIMIT=512M
SERVER_CPU_LIMIT=1.0
WEB_MEMORY_LIMIT=128M
WEB_CPU_LIMIT=0.5

# Backup Configuration
BACKUP_DIR=../backups
BACKUP_RETENTION_DAYS=30

# Monitoring
HEALTH_CHECK_INTERVAL=30s
HEALTH_CHECK_TIMEOUT=10s
HEALTH_CHECK_RETRIES=3

# =============================================================================
# NOTES
# =============================================================================
#
# 1. Never commit .env file to version control
# 2. Keep DELETION_TOKEN_PEPPER secure and backed up
# 3. Use different values for dev/staging/production
# 4. Restart services after changing environment variables:
#    docker compose down && docker compose up -d
